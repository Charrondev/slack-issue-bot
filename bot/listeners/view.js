module.exports = (controller, bot) => {
  controller.hears(['view'], ['direct_message', 'direct_mention'], (bot, message) => {
    // check issue number

    // post

    // Start conversation

    //
  });
}
