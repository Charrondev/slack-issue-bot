module.exports = (controller, bot) => {
  controller.hears(['list'], ['direct_message', 'direct_mention'], (bot, message) => {
    // Check if closed is specified

    // Check for labels

    // check for labels

    // check
  });
}
