module.exports = (controller, bot) => {
  controller.hears(['create'], ['direct_message', 'direct_mention'], (bot, message) => {
    // get title

    // check includes

    // conversation for details. Let
  });
}
